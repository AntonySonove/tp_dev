/*
// A1
// Initialisation
setPos(300, 300);
faceDown();
changeColor(color.red);
setLineWidth(10);

// Réalisation
forward(200);
faceRight();
forward(100);
*/

// A2
// Initialisation
/*
setPos(300, 300);
faceLeft();
changeColor(color.yellow);
forward(100);
faceDown();
forward(100);
faceRight();
forward(200);
faceUp();
forward(200);
*/

//A3
// Initialisation
/*
setPos(400, 300);
faceLeft();
changeColor(color.yellow);
forward(200);
setPos(300, 200);
faceDown();
forward(200);
*/

//A3
// Initialisation
/*
setPos(200, 500);
faceRight();
changeColor(color.green);
//réalisation exo
forward(200);
arcLeft(100, 180);
forward(100);
arcRight(100, 180);
forward(200);
*/

//A4
// Initialisation
/*
setPos(300, 100);
faceRight();
changeColor(color.green);
//réalisation exo
arcRight(200, 360)
*/

//B2
// Initialisation
/*
setPos(100, 300);
faceUp();
changeColor(color.red);
//réalisation exo

for(let i = 0 ; i < 4 ; i++) {
    forward(50);
    faceRight();
    forward(50);
    faceDown();
    forward(50);
    faceRight();
    forward(50);
    faceUp();
}
*/

//B3
// Initialisation
/*
setPos(100, 300);
faceRight();
changeColor(color.fuchsia);
//réalisation exo

for(let i = 0 ; i < 4 ; i++) {
    forward(100);
    right(90)
}
*/

//B4
// Initialisation
/*
setPos(100, 300);
faceRight();
changeColor(color.fuchsia);
//réalisation exo
for(let i = 0 ; i < 3 ; i++) {
    forward(100);
    left(180-60)
}
*/

//B5
// Initialisation
/*
setPos(300, 500);
faceUp();
changeColor(color.yellow);
//réalisation exo
forward(200);
changeColor(color.red);
for(let i = 0 ; i < 3 ; i++) {
    forward(100)
    right(180-60);
}
*/

//B6
// Initialisation
/*
setPos(100, 400);
faceRight();
changeColor(color.red);
//réalisation exo
for(let i = 0 ; i < 2 ; i++) {
    forward(50);
    faceUp();
    forward(50);
    faceRight();
}
changeColor(color.green);
for(let i = 0 ; i < 3 ; i++) {
    faceRight();
    forward(50);
    faceDown();
    forward(50);
}
*/

//B7
// Initialisation
/*
setPos(100, 400);
faceRight();
changeColor(color.yellow);
//réalisation exo
for(let i = 0 ; i < 4 ; i++) {
    forward(50);
    left(90);
}
setPos(100, 350);
for(let i = 0 ; i < 3 ; i++) {
    forward(50);
    left(180-60);
}
*/

//B8
// Initialisation
/*
setPos(100, 400);
faceRight();
changeColor(color.yellow);
//réalisation exo
for(let i = 0 ; i < 8 ; i++) {
    forward(200);
    left(180-45);
}
*/

//B9
// Initialisation
/*
setPos(100, 400);
faceRight();
changeColor(color.fuchsia);
//réalisation exo
forward(50);
setPos(200,400);
for(let i = 0 ; i < 3 ; i++) {
    forward(50);
    right(180-60);
}
*/

//B10
// Initialisation
/*
setPos(100, 300);
faceRight();
changeColor(color.blue);
//réalisation exo
for(let i = 0 ; i < 8 ; i++) {
    forward(50);
    right(180-45);
}
setPos(200, 300);
changeColor(color.yellow);
for(let i = 0 ; i < 8 ; i++) {
    forward(100);
    left(180-45);
}
setPos(175, 400);
changeColor(color.fuchsia);
for(let i = 0 ; i < 8 ; i++) {
    forward(50);
    right(180-45);
}
*/

//C2
// Initialisation
/*
setPos(100, 100);
faceDown();
changeColor(color.fuchsia);
//réalisation exo
for(let i = 0 ; i < 5 ; i++) {
    for(let i = 0 ; i < 4 ; i++) {
        forward(50);
        left(90);
    }
    up();
    faceDown();
    forward(100);
    down();
}
*/

//C3
// Initialisation
/*
setPos(50, 100);
faceRight();
shiftColor(0.43);
//réalisation exo
for(let i = 0 ; i < 3 ; i++) {
    for(let j = 0 ; j < 8 ; j++) {
        forward(50);
        left(180-45);
    }
    up();
    forward(100);
    shiftColor(0.1);
    down();
}
*/

//C4
// Initialisation
/*
setPos(50, 150);
faceRight();
changeColor(color.red);
//réalisation exo
for(let i = 0 ; i < 5 ; i++) {
    for(let j = 0 ; j < 3 ; j++) {
        changeColor(color.red);
        forward(50);
        left(180-60);
    }
    changeColor(color.yellow);
    for(let j = 0 ; j < 4 ; j++) {
        forward(50);
        right(90);
    }
    up();
    forward(100);
    down();
}
*/

//C5
// Initialisation
/*
setPos(200, 150);
faceRight();
shiftColor(0.2);
//réalisation exo
for(let i = 0 ; i < 4 ; i++) {
    for(let j = 0 ; j < 8 ; j++) {
        forward(100);
        left(180-45);
    }
    shiftColor(0.25);
right(180-90);
}
*/

//C6
// Initialisation
/*
setPos(200, 200);
faceRight();
changeColor(color.yellow);
//réalisation exo
for(let i = 0 ; i < 3 ; i++) {
    for(let j = 0 ; j < 4 ; j++) {
        forward(100);
        left(180-60);
    }
    forward(100);
}
*/

//D1
// Initialisation
/*
setPos(200, 200);
faceRight();
changeColor(color.red);
//réalisation exo
for(let i = 0 ; i < 8 ; i++) {
    forward(100);
    right(180-135)
}
*/

/*
//Déclaration
let longueur;

//Affectation
//Affecter une valeur à une variable : écrire la valeur dans la variable
longueur = 100;

//Initialisation : Déclaration + Affectation
let longueur = 100;

//Lecture
//Acceder au contenu de la variable
longueur;
100;

console.log(longueur);

//Incrémentation
longueur = longueur + 5;
console.log("La nouvelle valeur de longueur est", longueur);
*/

//D10
// Initialisation
/*
setPos(250, 250);
faceRight();
changeColor(color.red);
let longueur; 
longueur = 50;
//réalisation exo
for(let i = 0 ; i < 10 ; i++) {
    forward(longueur);
    left(90);
}
longueur = longueur + 100;
*/


// D9 (Spirale triangulaire)
// Initialisation
setPos(250, 300);
faceRight();
changeColor(color.red);
setLineWidth(5);
let longueur;

// Réalisation
longueur = 100;
for(let i = 0 ; i < 60 ; i++) {
    forward(longueur);
    right(120);
    longueur = longueur + 10;
    // Ou longueur += 100;
}